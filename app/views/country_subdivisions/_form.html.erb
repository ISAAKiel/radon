<%= form_for(@country_subdivision) do |f| %>
  <% if @country_subdivision.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_subdivision.errors.count, "error") %> prohibited this country_subdivision from being saved:</h2>

      <ul>
      <% @country_subdivision.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :country_id %><br>
    <%= f.text_field :country_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
